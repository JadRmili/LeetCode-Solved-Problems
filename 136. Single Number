class Solution:
    def singleNumber(self, nums: List[int]) -> int:
        if len(nums) == 1:
            return nums[0]
        else:
            for x in nums:
                if nums.count(x) == 1:
                    return x
