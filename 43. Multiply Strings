class Solution:
    def multiply(self, num1: str, num2: str) -> str:
        
        
        num1_int = 0
        num2_int = 0
        for x in num1:
            num1_int = num1_int * 10 + (ord(x) - 48)
        for x in num2:
            num2_int = num2_int * 10 + (ord(x) - 48)
        
        result = num1_int * num2_int
        
        return (str(result))
